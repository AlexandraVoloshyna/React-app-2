export enum CardActions {
  CREATE = "created",
  DELETE = "deleted",
  UPDATE = "updated",
  MOVE = "moved",
}

export enum ListActions {
  CREATE = "created",
  UPDATE = "updated",
  DELETE = "deleted",
}
